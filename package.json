{
  "name": "vault",
  "version": "0.1.3",
  "description": "File encryption and decryption cli tool written in go",
  "scripts": {
    "build": "go build -o vault cmd/main.go",
    "dev": "nodemon -w ./cmd -w ./internal -w ./pkg --ext *.go -x \"npm run test && npm run build\"",
    "test": "go test ./..."
  },
  "keywords": [
    "go",
    "golang",
    "vault"
  ],
  "author": "NobleMajo",
  "license": "MIT",
  "dependencies": {
    "nodemon": "^3.1.4"
  },
  "bin": {
    "vault": "./vault"
  }
}